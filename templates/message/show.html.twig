{% extends 'bootstrap_4.html.twig' %}

{% block title %}Message{% endblock %}

{% block content %}
    <main role="main" class="flex-shrink-0">
        <div class="container">
    <h1>Message</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ message.id }}</td>
            </tr>
            <tr>
                <th>Thetitle</th>
                <td>{{ message.thetitle }}</td>
            </tr>
            <tr>
                <th>Themessage</th>
                <td>{{ message.themessage }}</td>
            </tr>
            <tr>
                <th>Thedate</th>
                <td>{{ message.thedate ? message.thedate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('message_index') }}">back to list</a>

    <a href="{{ path('message_edit', {'id': message.id}) }}">edit</a>

    {{ include('message/_delete_form.html.twig') }}
    </div></main>
{% endblock %}
